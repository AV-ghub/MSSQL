Резкий скачок CPU до 90% при отсутствии очевидной нагрузки — классическая проблема, которая может иметь несколько простых в исправлении причин. Вот наиболее вероятные из них, с которыми сталкиваются многие администраторы.

### 🔍 Частые "тривиальные" причины резкого роста CPU

В таблице ниже приведены частые причины и простые действия для их проверки и устранения:

| Причина / Симптом | Проверка | Простое решение |
| :--- | :--- | :--- |
| **Неоптимальный Degree of Parallelism (DOP)**<br>Много простых запросов параллельно нагружают все ядра. | 1. Выполнить запрос: `SELECT TOP 25 * FROM sys.dm_os_wait_stats ORDER BY wait_time_ms DESC;`. Если в топе `CXPACKET` — есть проблема .<br>2. Проверить текущие настройки `max degree of parallelism`. | Установить `max degree of parallelism = 1` (или вручную на несколько ядер) как временную меру для снижения нагрузки . |
| **Устаревшая статистика**<br>Оптимизатор строит неверные планы, ведущие к сканированию таблиц вместо поиска по индексам . | 1. Найти самые ресурсоемкие запросы (см. запрос ниже).<br>2. Анализировать их планы выполнения — искать Table/Index Scan. | Обновить статистику для проблемных таблиц или для всей БД: `EXEC sp_updatestats;` . |
| **Массовая компиляция планов**<br>"Плохой" паттерн запросов вызывает лавину компиляций, потребляющих ЦП . | 1. Сравнить счетчики `SQL Compilations/sec` и `Re-Compilations/sec` до и после скачка (PerfMon).<br>2. Часто возникает после обновления CU в SQL 2012 (дефект MS) . | Для SQL 2012: установить соответствующий hotfix . Общая практика: найти и исправить источник непараметризованных запросов. |
| **Проблемы с лицензированием/ядром**<br>Сервер использует не все ядра, показывая 100% в мониторе активности, но меньше в Диспетчере задач . | Выполнить: `SELECT cpu_id, status, is_online FROM sys.dm_os_schedulers`. Наличие статуса `VISIBLE OFFLINE` указывает на проблему . | Включить настройку "Automatically set processor affinity mask" или проверить лицензионные ограничения . |
| **Внешние процессы**<br>Проблема не в SQL Server. | 1. В Диспетчере задач убедиться, что высокую нагрузку создает процесс `sqlservr.exe` .<br>2. Если `% Privileged Time` > 90% — проблема в драйверах/антивирусе . | Закрыть сторонние приложения, перенести службы (например, WorkFlow), обновить драйверы, исключить папки SQL из проверки антивирусом. |

### 🛠️ Как начать диагностику

1.  **Шаг 1: Убедиться, что виноват SQL Server**
    *   В Диспетчере задач проверьте, что процесс `sqlservr.exe` потребляет ~90% ЦП .
    *   Если нет, ищите проблему в других процессах (драйверы, антивирус, системные службы) .

2.  **Шаг 2: Найти конкретные "тяжелые" запросы**
    *   Используйте этот запрос, чтобы найти текущие или недавно выполненные запросы, потребляющие больше всего CPU:
    ```sql
    -- Текущие проблемные запросы
    SELECT TOP 10 s.session_id, r.status, r.cpu_time, r.logical_reads,
           SUBSTRING(st.text, (r.statement_start_offset/2) + 1,
           ((CASE r.statement_end_offset WHEN -1 THEN DATALENGTH(st.text)
             ELSE r.statement_end_offset END - r.statement_start_offset)/2) + 1) AS statement_text,
           s.login_name, s.host_name, s.program_name
    FROM sys.dm_exec_sessions AS s
    JOIN sys.dm_exec_requests AS r ON r.session_id = s.session_id
    CROSS APPLY sys.dm_exec_sql_text(r.sql_handle) AS st
    WHERE r.session_id != @@SPID
    ORDER BY r.cpu_time DESC;
    ```

    *   Для анализа исторических данных используйте `sys.dm_exec_query_stats` .

3.  **Шаг 3: Проверить базовые настройки**
    *   **Degree of Parallelism**: Установка `max degree of parallelism = 1` — это известный "быстрый фикс" для систем с множеством простых транзакций (например, типичных OLTP). Эффект часто заметен моментально .
    *   **Статистика**: Запустите `EXEC sp_updatestats;` .
    *   **Ядра**: Проверьте запрос к `sys.dm_os_schedulers` на наличие `VISIBLE OFFLINE` .

4.  **Шаг 4: Проанализировать планы запросов**
    *   Для найденных тяжелых запросов посмотрите план выполнения. Ищите:
        *   **Table Scan / Index Scan** (вместо Seek) — указывает на устаревшую статистику или отсутствующие индексы .
        *   **Огромные сортировки (Sort) или хэши (Hash Join)** в памяти — могут "съедать" CPU.

### 🧩 Вспоминаем ваш случай

Опираясь на ваш опыт, проанализируйте признаки. Возможно, это был один из этих сценариев:
*   **Если проблема решилась настройкой `max degree of parallelism`**, то до исправления в топе ожиданий был `CXPACKET`.
*   **Если помогло обновление статистики** — планы запросов были неоптимальны, но после обновления CPU упал.
*   **Если был установлен патч (hotfix)** — возможно, это был известный дефект с компиляцией в SQL Server 2012 .
