# Концепции
С 2016 года появилось много значимых новых возможностей, которые сильно развивают платформу SQL Server. Основные улучшения затронули производительность, безопасность, гибридную облачную интеграцию и управление данными. Давайте разберем их по версиям.

### 📊 Ключевые нововведения по версиям SQL Server

| Направление / Версия | SQL Server 2017 | SQL Server 2019 | SQL Server 2022 |
| :--- | :--- | :--- | :--- |
| **Производительность и оптимизация** | Возобновляемое перестроение индексов в сети; Адаптивная обработка запросов (adaptive joins); Автоматическая настройка | Интеллектуальная обработка запросов: **Пакетный режим для rowstore**; **Встраивание скалярных UDF**; Отложенная компиляция табличных переменных | **Упорядоченные кластеризованные индексы Columnstore**; Улучшенное исключение сегментов; **Параллельное сканирование буферного пула**; Улучшения `tempdb` |
| **Безопасность и управление** | Безопасность сборок CLR (`clr strict security`) | — | **Ledger (Бухгалтерская книга)** для криптографической верификации данных; **Бесплатная аутентификация через Microsoft Entra (ранее Azure AD)**; Детализированные разрешения и роли сервера; TDS 8.0 |
| **Гибридное облако и интеграция** | Первый выпуск **SQL Server на Linux**; Поддержка контейнеров | **Кластеры больших данных** (Big Data Clusters); Улучшенная **виртуализация данных через PolyBase** (MongoDB, Oracle) | **Azure Synapse Link** для аналитики в реальном времени; **Резервное копирование в хранилище, совместимое с S3**; **Связь с Управляемым экземпляром SQL Azure** для аварийного восстановления |
| **Управление данными** | Графовые базы данных для отношений "многие ко многим"; Новые функции строк (`STRING_AGG`, `TRIM`) | — | — |
| **Платформа и масштабирование** | Улучшения In-Memory OLTP | **Гибридный буферный пул** для PMEM; **Оптимизированные для памяти метаданные `tempdb`** для масштабируемости | — |

> **Важное примечание**: SQL Server 2016 завершил расширенную поддержку в июле 2026 года и больше не получает обновлений безопасности, что является серьезным риском для безопасности и соответствия требованиям.

### 🚀 Самые значимые "игро-меняющие" функции

Из всего разнообразия можно выделить несколько ключевых технологий, которые кардинально меняют подход к разработке и администрированию:

1.  **Интеллектуальная обработка запросов (с 2017 года, активно развивалась)**
    Это набор функций, которые **адаптируют план выполнения запроса на основе реальных данных среды выполнения**. Проще говоря, SQL Server "учится" в процессе работы.
    *   **Адаптивные соединения (Adaptive Joins)**: Оптимизатор может динамически переключаться между `Hash Match` и `Nested Loops` прямо во время выполнения, выбирая более эффективный алгоритм.
    *   **Встраивание скалярных UDF (Scalar UDF Inlining)**: Раньше скалярные функции были "черным ящиком" и убивали производительность. Теперь их код может встраиваться в основной запрос, что часто ускоряет выполнение в сотни раз.
    *   **Пакетный режим для rowstore**: Раньше сверхбыстрая пакетная обработка (`Batch Mode`) была доступна только для колоночных индексов. Теперь аналитические запросы к обычным rowstore-таблицам также могут использовать этот высокоэффективный режим.

2.  **Виртуализация данных с PolyBase (с 2019 года)**
    Позволяет выполнять запросы T-SQL к **внешним и разнородным источникам данных**, как будто это локальные таблицы. В 2019 году добавили поддержку MongoDB и Oracle, а в 2022 — совместимое с S3 хранилище. Теперь можно делать `JOIN` между таблицей в SQL Server и данными в Hadoop, Azure Blob или другом SQL-сервере.

3.  **Ledger (Бухгалтерская книга) в SQL Server 2022**
    Позволяет создать криптографически верифицируемую историю изменений данных. Любое изменение (INSERT, UPDATE, DELETE) автоматически записывается в защищенную от несанкционированного доступа таблицу журнала. Вы (или аудитор) можете в любой момент доказать, что данные не были подделаны. Это прорыв для финансовых, медицинских и любых других систем, требующих строгого аудита.

4.  **Гибридный буферный пул (SQL Server 2019)**
    Функция для серверов с энергонезависимой памятью (Persistent Memory, PMEM). Позволяет SQL Server обращаться к страницам данных напрямую в PMEM, минуя традиционный буферный пул в оперативной памяти. Это резко снижает задержки операций ввода-вывода.

5.  **Azure Synapse Link для SQL (SQL Server 2022)**
    Мост между операционными транзакционными данными в SQL Server и аналитикой в облаке. Позволяет **реплицировать данные в Azure Synapse Analytics почти в реальном времени** с минимальным влиянием на рабочую нагрузку основного сервера. Идеально для создания современных аналитических панелей без нагрузки на OLTP-систему.

### 💎 Краткие итоги по версиям

*   **SQL Server 2017**: Выход за пределы Windows (Linux, контейнеры), первые шаги к самонастраивающейся СУБД и графовые базы данных.
*   **SQL Server 2019**: Фокус на гибридные сценарии (большие данные, облако) и революционные улучшения в ядре обработки запросов для аналитики.
*   **SQL Server 2022**: Наиболее тесная интеграция с Azure, беспрецедентный уровень безопасности (Ledger, Entra) и дальнейшая оптимизация для современных аппаратных архитектур.

# Разработка
Синтаксис Transact-SQL (T-SQL) и подход к разработке баз данных на базе SQL Server 2016 изменились достаточно сильно. Основные нововведения можно разделить на две категории: новые ключевые слова и функции языка, а также эволюция инструментов и практик разработки (DevOps).

### 📝 Основные изменения в синтаксисе и возможностях T-SQL

Вот краткий обзор ключевых языковых возможностей, появившихся с 2016 года:

| Версия | Нововведение / Ключевое слово | Краткое описание |
| :--- | :--- | :--- |
| **2016+** | **`DROP IF EXISTS` / `CREATE OR ALTER`** | Упрощение скриптов: `DROP TABLE IF EXISTS dbo.MyTable;` и `CREATE OR ALTER PROCEDURE...` . |
| **2016** | **`STRING_SPLIT()`** | Разбивает строку с разделителями на таблицу. Ранее требовалась пользовательская функция. |
| **2016** | **Функции для работы с JSON** | `OPENJSON`, `FOR JSON` — преобразование табличных данных в JSON и обратно. |
| **2017** | **`STRING_AGG()`** | Обратная `STRING_SPLIT`: объединяет значения столбца в строку с разделителем. |
| **2017** | **`TRIM()`** | Удаляет пробелы или указанные символы с обоих сторон строки (заменяет связку `LTRIM(RTRIM())`). |
| **2019** | **`WITHIN GROUP` для `STRING_AGG()`** | Добавил сортировку элементов при агрегации. |
| **2019** | **Встраивание скалярных UDF** | Оптимизатор может "встроить" код скалярной функции в основной запрос для резкого ускорения. |
| **2022** | **`IS [NOT] DISTINCT FROM`** | Безопасное сравнение с `NULL`. `a IS NOT DISTINCT FROM b` аналогично `(a = b OR (a IS NULL AND b IS NULL))`. |
| **2022** | **`GREATEST()` / `LEAST()`** | Возвращает наибольшее/наименьшее значение из списка аргументов. |
| **2022** | **`WINDOW` clause** | Позволяет определить именованное окно для оконных функций, чтобы не дублировать код. |

### 🛠️ Эволюция в инструментах и практиках разработки (DevOps)

Изменения в инструментах и подходах к управлению схемой БД (Database DevOps) не менее важны:

1.  **Современный формат проектов `.sqlproj` (SDK Style)**: Старый, сложный `.sqlproj` файл, полный XML, уходит в прошлое. Новый формат гораздо компактнее, понятнее и может собираться без установленного Visual Studio, в том числе в Docker-контейнерах, что критично для CI/CD .
2.  **Публикация `dacpac` как артефактов NuGet**: Появилась возможность публиковать скомпилированный пакет схемы базы данных (`dacpac`) в репозиторий (например, NuGet). Это позволяет централизованно управлять общими компонентами и зависимостями между разными проектами БД .
3.  **Переход к Azure Data Studio и Visual Studio Code**: SQL Server Management Studio (SSMS) остается основным административным инструментом, но для разработки всё чаще используется **Visual Studio Code с расширением MSSQL**, особенно в кросс-платформенных средах .
4.  **Анализ кода T-SQL (TSQL Code Analysis)**: Разработчики могут создавать и подключать собственные статические анализаторы кода на C# для автоматической проверки стандартов кодирования и поиска потенциальных проблем прямо в проекте БД .

### 🚀 Главный контекст: окончание поддержки SQL Server 2016

Важно помнить, что **расширенная поддержка SQL Server 2016 завершится 14 июля 2026 года**. Это означает отсутствие обновлений безопасности и исправлений. Пора планировать переход на более новые версии, такие как **SQL Server 2022** или в облако (**Azure SQL Managed Instance**) .
