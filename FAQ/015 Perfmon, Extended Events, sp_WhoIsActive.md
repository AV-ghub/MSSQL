# Extended Events
Расширенные события (Extended Events, XE) — это современная, легковесная система диагностики в SQL Server, которая пришла на смену SQL Profiler. Она позволяет точно записывать "что происходит" в ядре СУБД с минимальным влиянием на производительность.

Чтобы сразу увидеть, как эти инструменты дополняют друг друга в диагностике, взгляните на эту таблицу:

| Инструмент | Основная роль при срочной диагностике "тупит" |
| :--- | :--- |
| **Perfmon (Системный монитор)** | **"Что?"** — Обнаруживает проблему на уровне ОС: высокий ЦП, диск, нехватку памяти. Ответ на вопрос "сервер в целом грузится?" |
| **`sp_WhoIsActive`** | **"Кто?"** — Показывает **прямо сейчас**: какие конкретные запросы выполняются, что блокирует, на чем ждут. |
| **Расширенные события (XE)** | **"Почему?"** и **История** — Записывает детальную последовательность событий, ведущих к проблеме (например, deadlock). Помогает воспроизвести и понять причину, если "следы остыли". |

Давайте рассмотрим, как использовать XE на практике.

### 🛠️ Практическое применение Extended Events

Основная суть работы с XE — создание **сеанса (session)**, который определяет:
1.  **Какие события (events)** отслеживать (например, завершение запроса, deadlock).
2.  **Какие дополнительные данные (actions)** собирать (текст SQL, имя приложения).
3.  **Куда выводить данные (target)** (файл, окно live-просмотра).
4.  **Какие фильтры (predicates)** применить (например, только запросы дольше 5 секунд).

#### Создание сеанса через GUI в SSMS
Это самый наглядный способ для начала работы.
1.  В **Object Explorer** разверните `Management` -> `Extended Events`.
2.  Щелкните правой кнопкой на `Sessions` и выберите `New Session...`.
3.  На вкладке **General** укажите имя сеанса (например, `MyDiagnostics`).
4.  На вкладке **Events** выберите события для отслеживания (см. ниже).
5.  Нажмите кнопку **Configure** рядом с выбранным событием, чтобы добавить `Actions` (дополнительные поля, например, `sqlserver.sql_text`) и `Filters` (например, `duration > 5000000` для запросов >5 секунд).
6.  На вкладке **Data Storage** выберите **Target**. Для старта подойдет `event_file`, чтобы писать данные в `.xel` файл на диск.
7.  Нажмите **ОК** и запустите сеанс.

#### Полезные события для диагностики производительности
Вот ключевые события, с которых стоит начать:

| Событие (Event) | Что фиксирует | Для чего использовать |
| :--- | :--- | :--- |
| **`sql_statement_completed`** | Завершение каждого SQL-запроса с **метриками**: длительность, ЦП, логические/физические чтения. | Поиск самых ресурсоемких запросов. Добавляйте фильтр по `duration` или `cpu_time`. |
| **`rpc_completed`** | Завершение вызова RPC (чаще всего `sp_executesql` от клиентов вроде 1С). | Анализ нагрузки от конкретных приложений. |
| **`xml_deadlock_report`** | Возникновение deadlock'а с полным графом блокировок. | Анализ причин взаимоблокировок. |
| **`lock_*`** (acquired, released и т.д.) | Операции с блокировками. | Глубокий анализ проблем блокировок и contention. |
| **`wait_info`** | События ожиданий. | Детализация ожиданий, отличная от агрегированной статистики `sys.dm_os_wait_stats`. |

#### Пример: Создание сеанса для поиска тяжелых запросов через T-SQL
Этот скрипт создает сеанс, который записывает в файл запросы, выполняющиеся дольше 5 секунд, вместе с их текстом и данными о клиенте.

```sql
CREATE EVENT SESSION [HeavyQueries] ON SERVER
ADD EVENT sqlserver.sql_statement_completed(
    ACTION(
        sqlserver.sql_text,
        sqlserver.client_app_name,
        sqlserver.client_hostname,
        sqlserver.database_name
    )
    WHERE ([duration] > 5000000) -- 5 000 000 микросекунд = 5 секунд
)
ADD TARGET package0.event_file(
    SET filename = N'C:\Temp\HeavyQueries.xel', -- Укажите свой путь
    max_file_size = 100, -- MB
    max_rollover_files = 5
)
WITH (
    MAX_MEMORY = 4 MB,
    EVENT_RETENTION_MODE = ALLOW_SINGLE_EVENT_LOSS, -- Минимизация влияния на производительность
    MAX_DISPATCH_LATENCY = 30 SECONDS
);
GO
-- Запустить сеанс
ALTER EVENT SESSION [HeavyQueries] ON SERVER STATE = START;
```

#### Анализ собранных данных
После того как сеанс поработал и собрал данные, их можно проанализировать:

1.  **Live Data (прямо в SSMS)**: Щелкните правой кнопкой на работающем сеансе и выберите `Watch Live Data`. События будут отображаться в реальном времени. В окне просмотра можно группировать данные и применять агрегатные функции (SUM, COUNT), что полезно для анализа.
2.  **Чтение из файла `.xel`**: Данные в файле хранятся в XML-подобном формате. Их можно распарсить с помощью T-SQL:
    ```sql
    -- Чтение данных из файла
    SELECT
        CAST(event_data AS XML) AS EventData_XML
    FROM sys.fn_xe_file_target_read_file(
        'C:\Temp\HeavyQueries*.xel', -- * для чтения всех файлов rollover
        NULL, NULL, NULL
    );
    ```
    Для удобства можно распарсить XML и представить данные в табличном виде.

### 💎 Сводная стратегия диагностики "на лету"

1.  **Первичный осмотр**: Запустите `sp_WhoIsActive`. Есть ли явные "виновники"? Если нет, но система тормозит, переходим к шагу 2.
2.  **Анализ ожиданий**: Проверьте агрегированную статистику через `sys.dm_os_wait_stats`. Какие ожидания лидируют? Если это `LCK_M_*` (блокировки), можете сразу создать и запустить XE-сеанс на события `lock_*` или `xml_deadlock_report` для сбора деталей.
3.  **Анализ ресурсов ОС**: Одновременно откройте Perfmon. Если видите проблемы с диском (`Avg. Disk sec/Read > 15-20ms`), а в `sp_WhoIsActive` много ожиданий `PAGEIOLATCH_*`, создайте XE-сеанс на `sql_statement_completed` с фильтром по `physical_reads`, чтобы найти запросы, вызывающие максимальную нагрузку на I/O.
4.  **Запись для истории**: Если проблема непостоянная ("раз в день на час тупит"), настройте **постоянный сеанс XE** с записью в файл, который стартует автоматически со SQL Server. Собранные файлы позже можно будет проанализировать.

### 🚀 Полезные подсказки

*   **Используйте системный сеанс `system_health`**: Он уже работает по умолчанию и автоматически собирает информацию о deadlock'ах, серьезных ошибках и долгих ожиданиях. Его данные — первое место, где стоит искать следы уже произошедших проблем.
*   **Настройте `EVENT_RETENTION_MODE`**: Используйте `ALLOW_SINGLE_EVENT_LOSS` (по умолчанию) для минимального влияния на производительность. Используйте `NO_EVENT_LOSS` только в критически важных сценариях отладки, понимая, что это может создать нагрузку.

### 🎯 Вывод

Extended Events — это **инструмент для точной, детальной и исторической диагностики**. В горячей ситуации вы сначала используете `sp_WhoIsActive` и Perfmon, чтобы понять "что" и "кто", а затем подключаете XE, чтобы зафиксировать и понять глубинную причину "почему".

