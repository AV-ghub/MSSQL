## 🧭 Этап 13: Выбор между FCI и AG — стратегия и тактика

Выбор между FCI и AG (или их комбинацией) определяется не личными предпочтениями, а **четкими техническими требованиями и ограничениями среды**.

### **1. Ключевые критерии выбора**

Сравнительная таблица по основным критериям:

| Критерий выбора | Когда выбирать **Failover Cluster Instance (FCI)** | Когда выбирать **Availability Group (AG)** | Комментарий |
| :--- | :--- | :--- | :--- |
| **Уровень защиты** | **Экземпляр SQL Server и ОС**. Защищает от сбоя сервера, ОС, службы SQL. | **База данных (одна или несколько)**. Защищает от сбоя БД, экземпляра, сервера, ОС. | AG защищает на более детальном уровне. |
| **Хранилище** | **Обязательно общее (SAN, iSCSI)**. Это единая точка отказа, если не реплицируется на уровне массива. | **Локальное на каждом узле**. Не требует дорогого общего хранилища. | Если общего хранилища нет — AG единственный вариант HA. |
| **Реплика данных** | **Нет**. Данные в единственном экземпляре на общем хранилище. | **Да**. Каждая реплика — полная копия данных. | AG обеспечивает избыточность данных на уровне СУБД. |
| **Использование ресурсов** | Пассивные узлы простаивают (в активной-пассивной конфигурации). | Вторичные реплики можно использовать для **чтения, резервного копирования, обработки отчетов**. | AG даёт **горизонтальное масштабирование на чтение**. |
| **Географическое распределение (DR)** | Сложно и дорого (требует stretched cluster или репликации хранилища). | **Нативно поддерживается**. Можно создать асинхронную реплику в другом ЦОДе с умеренными сетевыми требованиями. | AG — лучший выбор для DR между удаленными площадками. |
| **Влияние на приложение при failover** | Все соединения разрываются. Клиенты должны переподключаться к тому же виртуальному имени. | Все соединения разрываются. Клиенты должны переподключаться к тому же **прослушивателю (Listener)**. | С точки зрения приложения процесс схож. |
| **Сложность настройки и администрирования** | Средняя. Сильно зависит от сложности хранилища и настройки кластера. | **Выше средней**. Требует управления репликацией данных, мониторинга лага, согласованности вторичных реплик. | AG вводит больше "движущихся частей". |
| **Типичный RPO/RTO** | RPO ≈ 0 (данные на общем диске), RTO ~ несколько минут. | **RPO = 0** (при синхронной репликации), **RTO ~ минуты** (при автоматическом failover). | Оба могут обеспечить нулевую потерю данных. |

### **2. Типовые архитектурные решения (Шаблоны)**

На практике чистые сценарии встречаются редко. Чаще используются комбинированные.

**Шаблон 1: "Классическая высокая доступность в ЦОДе"**
*   **Решение:** **FCI из двух узлов** с общим хранилищем.
*   **Почему:** Простота, знакомство администраторам, минимальное количество компонентов. Все системные БД (master, msdb) защищены вместе с пользовательскими.
*   **Недостаток:** Простаивающий ресурс пассивного узла.

**Шаблон 2: "Высокая доступность + масштабирование на чтение"**
*   **Решение:** **AG из 3+ реплик** в одном ЦОДе. Одна первичная (для записи), две или более синхронные вторичные (для чтения и автоматического failover).
*   **Почему:** Максимальная отказоустойчивость и использование ресурсов всех серверов.
*   **Пример:** Веб-портал, где нагрузка чтения в 10 раз превышает запись.

**Шаблон 3: "Высокая доступность на площадке + аварийное восстановление" (Наиболее популярный)**
*   **Решение:** **FCI на основной площадке** (для HA) + **AG между этим FCI и standalone сервером (или другим FCI) на DR-площадке**.
*   **Почему:** Комбинирует сильные стороны:
    1.  **FCI локально** — простое и быстрое переключение при сбое сервера/ОС, защита системных БД.
    2.  **AG для DR** — эффективная репликация только пользовательских данных на удалённую площадку с контролем RPO/RTO.
*   **Архитектура:** `FCI (Node1 + Node2)` --(AG)--> `Standalone Server in DR Site`.

**Шаблон 4: "Распределенная группа доступности (Distributed AG)"**
*   **Решение:** Две независимые AG в разных географических локациях, объединенные в распределенную AG.
*   **Почему:** Для миграции между ЦОДами или облаками с минимальным простоем, или для организации DR между полностью независимыми кластерами (разные домены, даже Windows/Linux).

### **3. Практические рекомендации для собеседования**

Если на собеседовании вас спросят: *"Как бы вы спроектировали высокодоступную инфраструктуру для нашей критичной базы данных?"* — начните с **уточняющих вопросов**:
1.  **"Какой у вас бюджет на хранилище? Есть ли готовая SAN?"** (Ответ определит возможность FCI).
2.  **"Требуется ли использовать резервные серверы для отчетов или резервного копирования?"** (Покажет потребность в readable secondary из AG).
3.  **"Каковы требования к RPO и RTO для этой БД?"** (Определит необходимость синхронной репликации и автоматического failover).
4.  **"Планируется ли аварийное восстановление в другой ЦОД?"** (Сразу склонит чашу весов в сторону AG или гибридной схемы).

**Ваш итоговый ответ должен звучать как обоснованное предложение,** например:
*"Исходя из того, что общее хранилище есть, а требования к RTO/RTO строгие, я бы предложил двухузловой FCI для основной площадки, чтобы быстро закрыть сбои сервера. И поверх него настроил бы асинхронную группу доступности на сервер в резервном ЦОДе для защиты от катастрофы на основной площадке. Это даст нам баланс между скоростью локального переключения и защитой данных на расстоянии."*

---

## 📚 Этап 14: Финальный рекап и контрольные точки

Мы прошли полный цикл от основ до архитектурных решений. Давайте закрепим ключевые контрольные точки, которые нужно помнить о WSFC, FCI и AG:

1.  **WSFC — это платформа.** Её нужно сначала подготовить (права AD, сети, свидетели), и только потом на неё ставить FCI или AG.
2.  **FCI и AG решают разные задачи.** FCI — это **HA для экземпляра**, AG — **HA и DR для базы данных**.
3.  **Кворум — основа стабильности.** Потеря кворума = остановка всех кластеризованных ролей. Всегда настраивайте свидетеля (Cloud Witness — лучший выбор).
4.  **Принудительный failover в AG — это операция с потерей данных.** Используйте её только в сценарии аварийного восстановления, когда первичная реплика безвозвратно утеряна.
5.  **Мониторинг лага (`redo_queue_size`, `log_send_queue_size`) — это мониторинг вашего реального RPO.** Без него вы работаете вслепую.
6.  **Гибридная архитектура (FCI + AG) — золотой стандарт** для критичных нагрузок, где есть и общее хранилище, и требование к DR.
