Настройка автоматического переключения при сбое (Automatic Failover) в AlwaysOn Availability Groups (AG) — это ключевой шаг для достижения высокой доступности с минимальным RTO. Для его работы необходимо выполнить три условия, перечисленные в таблице ниже:

| Условие для автоматического переключения | Пояснение |
| :--- | :--- |
| **1. Синхронный режим фиксации** | Все реплики, участвующие в паре автоматического переключения, должны использовать режим `SYNCHRONOUS_COMMIT`. |
| **2. Режим переключения «Автоматический»** | Реплики должны иметь `FAILOVER_MODE = AUTOMATIC`. |
| **3. Синхронизированное состояние** | Вторичная реплика должна находиться в состоянии `SYNCHRONIZED`. |

Настроить это можно двумя основными способами, как на этапе создания AG, так и после:

### 📝 1. Настройка в мастере создания группы доступности (SSMS)
В SQL Server Management Studio при создании AG в окне «Указание реплик» (Specify Replicas) нужно:
1. Для нужной вторичной реплики выбрать **Синхронная фиксация (Synchronous commit)** в колонке *Режим доступности (Availability Mode)*.
2. В колонке *Автоматический переход на другой ресурс (Automatic failover)* выбрать **Да**.

### ⚙️ 2. Настройка с помощью Transact-SQL
Если группа уже создана, используйте команду `ALTER AVAILABILITY GROUP`:
```sql
-- Изменить режим доступности и переключения для существующей реплики
ALTER AVAILABILITY GROUP [YourAGName]
MODIFY REPLICA ON N'YourSecondaryServer'
WITH (  
    AVAILABILITY_MODE = SYNCHRONOUS_COMMIT,  
    FAILOVER_MODE = AUTOMATIC  
);
```

> **Важно:** Автоматическое переключение возможно только между двумя репликами (первичной и одной вторичной), которые удовлетворяют всем трём условиям. Максимальное количество синхронных реплик зависит от версии SQL Server (например, в SQL Server 2019 — до 5).

### 🛠️ Дополнительные параметры политики переключения
Для тонкой настройки условий, при которых срабатывает автоматическое переключение, можно задать «гибкую политику»:
- **`FAILURE_CONDITION_LEVEL`** (от 1 до 5): определяет тяжесть ошибки, при которой запускается переключение. Уровень 1 — только отключение службы SQL Server (наименее строгий), уровень 5 — любые квалифицированные условия (самый строгий).
- **`HEALTH_CHECK_TIMEOUT`**: определяет, как долго кластер будет ждать ответа от реплики о её работоспособности (по умолчанию 30 секунд).

Изменить эти настройки можно в T-SQL:
```sql
ALTER AVAILABILITY GROUP [YourAGName]
SET (
    FAILURE_CONDITION_LEVEL = 3, -- Значение по умолчанию
    HEALTH_CHECK_TIMEOUT = 30000
);
```

### ⚠️ Что блокирует автоматическое переключение?
Система не выполнит автоматическое переключение, если:
* Целевая вторичная реплика не находится в состоянии `SYNCHRONIZED`.
* Одна из реплик в паре находится в режиме асинхронной фиксации (`ASYNCHRONOUS_COMMIT`) или ручного переключения (`FAILOVER_MODE = MANUAL`).
* Кластер Windows (WSFC) потерял кворум.
* Проблема возникла на уровне отдельной базы данных (например, база данных помечена как подозрительная), а не на уровне всей реплики или экземпляра (если не включено обнаружение работоспособности на уровне БД).

### 🧪 Рекомендации для собеседования
На собеседовании, объясняя эту тему, акцентируйте внимание на **трех обязательных условиях** и понимании разницы между:
* **Плановым ручным переключением (planned manual failover)** — администратор инициирует его при синхронизированных синхронных репликах (без потерь данных).
* **Принудительным переключением (forced failover)** — используется в аварийных ситуациях для асинхронных реплик или при потере первичной (риск потери данных).
* **Автоматическим переключением** — система срабатывает сама при соблюдении всех настроенных условий.
