### 📘 Этап 4: Кворум (Quorum) и Свидетель (Witness) — принцип работы

**Зачем это нужно?**
Кворум — это алгоритм голосования, который предотвращает "раскол мозга" (split-brain). Он отвечает на вопрос: *"Какая часть кластера имеет право продолжать работу при потере связи между узлами?"*

Принцип прост: кластер продолжает работу **только если у него есть большинство голосов ("кворум")**.

**Ключевые понятия:**
*   **Голос (Vote):** Каждый узел кластера по умолчанию имеет один голос.
*   **Свидетель (Witness):** Дополнительный голосующий ресурс, который не является узлом кластера. Его цель — **сделать количество голосов в системе НЕЧЁТНЫМ** (1, 3, 5...). Это гарантирует, что при любом разделении кластера одна из сторон гарантированно получит большинство (>50%).

> ⚠️ **Запомните:** Кластер, потерявший кворум, **останавливает все свои роли** (включая SQL Server AG или FCI) в качестве защитной меры, чтобы не было двух активных первичных реплик.

---

### 🧮 Этап 5: Конфигурация кворума — модели и расчеты

**Как распределяются голоса?** Рассмотрим на примере самого популярного сценария — **2 узла**.
- Узел 1 = 1 голос
- Узел 2 = 1 голос
- **Итого: 2 голоса.** **Проблема:** При потере связи (например, сетевого кабеля) каждый узел видит только себя (1 голос из 2). Это **50% голосов — не большинство**. Обе части кластера потеряют кворум и остановятся.

**Решение:** Добавить **Свидетеля (Witness)**, который даст 3-й голос.
- Узел 1 = 1 голос
- Узел 2 = 1 голос
- Свидетель = 1 голос
- **Итого: 3 голоса (нечётное число).**
- Теперь при разрыве связи, узел, который сохранит связь со **Свидетелем**, получит 2 голоса из 3 (66% — **большинство**) и продолжит работу. Вторая часть (1 голос) остановится.

**Формула для определения кворума:**
> **Большинство голосов = (Общее число голосов / 2) + 1**

Для кластера из 3 узлов (без свидетеля) кворум = 2 голоса. Если один узел отвалится, оставшиеся два (2 голоса) сохранят кворум и продолжат работу.

---

### 🛠️ Этап 6: Типы свидетелей (Witness) и их настройка

Существует три основных типа свидетелей, которые вы выбираете в зависимости от среды. Вот их сравнительная таблица:

| Тип Witness | Описание и принцип работы | Когда использовать | Основные "подводные камни" |
| :--- | :--- | :--- | :--- |
| **Файловый свидетель (File Share Witness, FSW)** | Общая сетевая папка на файловом сервере (вне кластера). Кластер записывает в неё небольшой файл с меткой времени. Узел, который последним успешно обновил этот файл, считается имеющим голос свидетеля. | **Стандартный выбор для кластеров в одном ЦОДе** или между ЦОДами с надежной сетью. Самый простой в настройке и понимании. | Файловый сервер — это **единая точка отказа (SPOF)**. Если он недоступен, свидетель теряет голос. Требует стабильной сети. |
| **Свидетель диска (Disk Witness)** | Выделенный диск в **общем хранилище кластера** (например, маленький LUN на SAN). | **Только для FCI (Failover Cluster Instance)**, так как у них есть общее хранилище. **Не используется для AG**, так как у AG общего диска на уровне кластера нет. | Не работает для AG. Общий диск сам по себе может стать единой точкой отказа. |
| **Облачный свидетель (Cloud Witness)** | Использует общедоступный контейнер **Azure Blob Storage** как арбитр. Кластеры записывают в него свои "heartbeat"-сообщения. | **Идеален для:** 1) Распределенных кластеров между ЦОДами. 2) Гибридных сред (часть узлов в Azure, часть локально). 3) Когда нет выделенного файлового сервера. | Требует **исходящего интернет-доступа** с узлов кластера к `*.core.windows.net`. Небольшая ежемесячная плата за хранение и операции с Blob Storage. |

> 💡 **Рекомендация для собеседования:** Если вас спросят, какой свидетель выбрать для AG из двух узлов, лучшим ответом будет **Облачный свидетель (Cloud Witness)**, так как он устраняет единую точку отказа, которую представляет файловый сервер. Если интернета нет — используйте Файловый свидетель.

### **Как настроить кворум через PowerShell (на примере Cloud Witness):**

1.  **Создайте учетную запись хранения Azure и контейнер Blob Storage.** Скопируйте ключ доступа (Access Key) и URL конечной точки.
2.  **Настройте на узлах кластера:** Убедитесь, что есть исходящий доступ к `*.core.windows.net` через порт 443 (HTTPS).
3.  **Выполните команду на одном из узлов кластера:**
    ```powershell
    Set-ClusterQuorum -Cluster "YourClusterName" -CloudWitness -AccountName "YourAzureStorageAccount" -AccessKey "YourAccessKey" -Endpoint "core.windows.net"
    ```

### **Как проверить текущую конфигурацию кворума:**
```powershell
Get-ClusterQuorum -Cluster "YourClusterName"
```
Эта команда покажет все голосующие узлы и тип настроенного свидетеля.

---

### 🚨 Этап 7: Подводные камни и аварийные ситуации с кворумом

1.  **Потеря кворума (самая частая проблема):** Если кластер теряет кворум, все роли (включая AG) останавливаются.
    *   **Причина:** Чаще всего — **проблемы с сетью**, из-за которых узлы теряют связь друг с другом и со свидетелем.
    *   **Диагностика:** В событиях Windows (Event Viewer) на узлах кластера ищите события с ID **1135, 1069, 1205** от источника **FailoverClustering**.
    *   **Экстренное решение (Force Quorum):** Если вы уверены, какая часть кластера должна выжить (например, основная площадка), можно **принудительно запустить её с кворумом**. Это делается через PowerShell на "живом" узле, который хотим сохранить:
        ```powershell
        # ОСТОРОЖНО! Используйте только при полном понимании последствий.
        # 1. Остановите кластерную службу на узле, который хотим сохранить:
        Stop-Service ClusSvc -Force
        # 2. Запустите узел в режиме принудительного кворума:
        Start-ClusterNode -Node "SurvivingNode" -FixQuorum
        # 3. После запуска переконфигурируйте кворум (например, удалите голоса отключенных узлов).
        ```

2.  **Неправильная настройка голосов:** Не всем узлам нужно давать голос. Например, узлу, который находится в сайте DR и служит только асинхронной репликой AG, **голос лучше отнять**, чтобы его отключение не влияло на кворум основной площадки.
    *   **Как отнять голос через PowerShell:**
        ```powershell
        (Get-ClusterNode -Name "DR-Node").NodeWeight = 0
        ```
    *   **После изменения веса обязательно пересчитайте кворум:**
        ```powershell
        (Get-Cluster).QuorumResource = (Get-Cluster).QuorumResource
        ```

