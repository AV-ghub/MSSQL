## 🧱 Этап 8: Создание и начальная настройка кластера WSFC

Этот этап — формальное объединение подготовленных серверов в единый управляемый объект.

### **1. Создание кластера**

Создать кластер можно, как и раньше, двумя путями: через графический интерфейс Failover Cluster Manager или PowerShell. Вариант с PowerShell предпочтительнее для точности и воспроизводимости.

**A. Создание кластера через PowerShell:**
```powershell
# Базовая команда создания кластера из двух узлов
New-Cluster -Name "ProdSQLCluster" -Node "SQLNode01", "SQLNode02" -StaticAddress 192.168.1.100 -NoStorage
```
*   **`-Name "ProdSQLCluster"`**: Это **имя кластера (Cluster Network Name, CNN)**. Оно будет зарегистрировано в DNS и станет точкой управления кластером.
*   **`-Node`**: Список серверов-участников.
*   **`-StaticAddress`**: **Статический IP-адрес для имени кластера**. Это не IP отдельного сервера, а виртуальный адрес самого кластера. Он должен быть **свободен в сети**.
*   **`-NoStorage`**: **Критически важный параметр для AG!** Он указывает не добавлять в кластер обнаруженные общие диски. Для AlwaysOn Availability Groups общее хранилище не требуется, и его случайное добавление только запутает конфигурацию. Для FCI этот параметр не используется — диски нужны.

**B. Создание кластера через Failover Cluster Manager:**
1.  Запустите оснастку `Failover Cluster Manager`.
2.  Выберите **"Create Cluster..."**.
3.  В мастере укажите имена серверов-узлов.
4.  В поле **"Access point for administering the cluster"** укажите имя кластера (`ProdSQLCluster`) и **назначьте ему статический сетевой IP-адрес**.
5.  На шаге подтверждения **обязательно отметьте галочку "Add all eligible storage to the cluster" как НЕактивную**, если создаете кластер для AG (аналог `-NoStorage`).

### **2. Что происходит "под капотом" при создании кластера**

В момент выполнения команды `New-Cluster` происходит несколько важных вещей:
1.  **Создается Cluster Name Object (CNO) в Active Directory.** Это учетная запись компьютера, представляющая кластер. У сервера, на котором запускается создание, должны быть права на создание объектов в OU (обычно требуется членство в группе `Domain Admins` или делегирование прав).
2.  **Регистрируется запись в DNS** для имени кластера (`ProdSQLCluster`) и его IP-адреса.
3.  **Настраивается внутренняя коммуникация** между узлами, формируется база данных конфигурации кластера (реестр), которая реплицируется на все узлы.
4.  **Создаются сетевые профили.** Система автоматически определит сетевые интерфейсы и создаст для них объекты в кластере (например, `Cluster Network 1`, `Cluster Network 2`). Их нужно будет правильно переименовать.

### **3. Настройка сетей кластера (Critical Step!)**

После создания кластера **первым делом нужно настроить сети**. По умолчанию все сети используются и для клиентского трафика, и для служебной коммуникации узлов (heartbeat). Это неоптимально.

**Задача:** Разделить сети. Например:
*   **"ClientNet"** (клиентская сеть) — для доступа приложений и управления. Оставить разрешение **"Allow clients to connect"**.
*   **"HeartbeatNet"** (служебная сеть) — только для внутренней коммуникации узлов (например, выделенный кросс-овер кабель или отдельная VLAN). Для этой сети нужно **отключить** `Allow clients to connect` и оставить только `Allow cluster network communication`.

**Как настроить через PowerShell:**
```powershell
# 1. Получить список сетей кластера
Get-ClusterNetwork -Cluster "ProdSQLCluster"

# 2. Переименовать сеть для удобства (пример)
(Get-ClusterNetwork -Cluster "ProdSQLCluster" -Name "Cluster Network 2").Name = "HeartbeatNet"

# 3. Для служебной сети отключить доступ клиентам
Get-ClusterNetwork -Cluster "ProdSQLCluster" -Name "HeartbeatNet" | Set-ClusterNetwork -Role 0
# Role: 3 = All (клиенты + кластер), 1 = Only кластер, 0 = None
```

### **4. Подводные камни при создании кластера**

1.  **"Не удалось создать объект имени кластера в домене"** — самая частая ошибка.
    *   **Причина:** У учетной записи, запускающей создание, нет прав на создание компьютерных объектов в целевом подразделении (OU) домена.
    *   **Решение:** Попросить AD-администратора **предварительно создать (pre-stage)** объект компьютера с именем кластера (`ProdSQLCluster`) и предоставить вам права на его "владение" (`Full Control`). Или запускать создание из-под учетной записи с необходимыми правами.

2.  **Проблемы с DNS:** Обратное разрешение имен (PTR-запись) для IP-адреса кластера или узлов работает некорректно. Всегда проверяйте `nslookup` в обе стороны.

3.  **Сеть Heartbeat проходит через маршрутизаторы:** Heartbeat-трафик очень чувствителен к задержкам. Идеально — выделенная физическая сеть точка-точка. Если это отдельная VLAN, убедитесь в стабильности и низкой задержке.

### **5. Проверка работоспособности базового кластера**

После настройки выполните базовые проверки:
```powershell
# Проверить состояние узлов
Get-ClusterNode -Cluster "ProdSQLCluster"

# Проверить состояние сетей
Get-ClusterNetwork -Cluster "ProdSQLCluster"

# Запустить быстрый тест работоспособности
Test-Cluster -Cluster "ProdSQLCluster" -List
```

**На этом этапе у вас должен быть работающий, чистый кластер WSFC с настроенными сетями и без лишних ресурсов (дисков).**
