### 🧭 План действий при аварии в основном ЦОДе
Цель: **восстановить работу приложений в резервном ЦОДе в кратчайшие сроки**, осознавая риск потери данных (RPO > 0).

1.  **Детектирование и оценка сбоя**:
    *   **Подтвердите невозможность восстановления основного ЦОД** в рамках вашего RTO.
    *   **Определите наименее отстающую вторичную реплику** в резервном ЦОДе. Подключитесь к её экземпляру SQL Server и выполните запрос, чтобы узнать размер очереди повтора (`redo_queue_size`) — он показывает, сколько данных не применено:
        ```sql
        -- Запрос на ВТОРИЧНОЙ реплике
        SELECT ar.replica_server_name,
               db_name(drs.database_id) as [База данных],
               drs.redo_queue_size, -- КБ данных, ожидающих применения (чем меньше, тем лучше)
               drs.last_redone_time
        FROM sys.dm_hadr_database_replica_states drs
        JOIN sys.availability_replicas ar ON drs.replica_id = ar.replica_id
        WHERE drs.is_local = 1
        ORDER BY redo_queue_size;
        ```
    *   Выберите реплику с наименьшим `redo_queue_size`. Чем он меньше, тем меньше данных будет потеряно, и тем быстрее завершится фаза восстановления.

2.  **Инициирование принудительного переключения**:
    *   Подключитесь к выбранной **вторичной реплике**.
    *   Выполните команду принудительного переключения, явно указывая на риск потери данных:
        ```sql
        -- ВНИМАНИЕ: Эта команда выполняется на целевой ВТОРИЧНОЙ реплике
        ALTER AVAILABILITY GROUP [Имя_Вашей_AG] FORCE_FAILOVER_ALLOW_DATA_LOSS;
        ```
    *   Команда немедленно переведет эту реплику в роль **первичной**. Все данные, которые не успели передаться с основной реплики, будут **безвозвратно утеряны** (определяет ваш итоговый RPO).

3.  **Перенастройка группы доступности**:
    *   Новая первичная реплика теперь работает в резервном ЦОДе, но старые реплики (включая бывшую первичную в основном ЦОДе) считаются неактуальными.
    *   **Удалите старые реплики из конфигурации AG** (через SSMS или T-SQL) на новой первичной, чтобы избежать конфликтов при попытке их повторного присоединения.
    *   Переконфигурируйте кворум кластера WSFC, если требуется (например, измените голоса узлов или настройте свидетеля файлового ресурса в резервном ЦОДе).

4.  **Проверка приложений**:
    *   Убедитесь, что все **приложения подключаются через имя прослушивателя (Listener)** группы доступности.
    *   Проверьте строки подключения: они должны содержать параметр `MultiSubnetFailover=True` для быстрого переподключения.
    *   Протестируйте ключевые функции приложений.

### 🛠️ Дальнейшие действия
После того как основной ЦОД восстановлен, вам потребуется **полностью перестроить среду AG**:
1.  Создайте полную резервную копию новой "первичной" базы данных в резервном ЦОДе.
2.  Восстановите её на серверах в основном ЦОДе с параметром `WITH NORECOVERY`.
3.  Добавьте эти серверы в группу доступности в качестве новых вторичных реплик.
4.  Запланируйте обратное переключение (плановый ручной failover) в окно обслуживания, когда основной ЦОД будет готов.

> **Важно**: В этой аварийной процедуре ключевой компромисс — это **сознательный выбор между доступностью (низкий RTO) и целостностью данных (RPO=0)**. Принудительный failover — это решение в пользу доступности.
