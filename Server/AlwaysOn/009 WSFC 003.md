### ⚙️ Шаг 1: Установка роли Failover Clustering
Основное действие — установка на каждый сервер (узел) роли Windows Server под названием **Failover Clustering**. Сделать это можно двумя основными способами.

**A. Через PowerShell (рекомендуется для автоматизации):**
```powershell
# Основная команда установки роли кластеризации
Install-WindowsFeature -Name Failover-Clustering -IncludeManagementTools
```
Ключевой параметр `-IncludeManagementTools` добавляет графическую консоль управления и необходимые модули PowerShell.

**B. Через графический интерфейс (Server Manager):**
В **Диспетчере серверов** (Server Manager) выполните `Добавить роли и компоненты` → выберите установку на основе ролей → отметьте роль **Failover Clustering** и подтвердите установку.

> ⚠️ **Важный нюанс:** Начиная с Windows Server 2016/2019 появилась необходимость после установки роли увеличить стандартный лимит размера пакетов WS-Management с 500 КБ до, например, 1024 КБ, чтобы избежать проблем с коммуникацией в кластере:
> ```powershell
> Set-WSManInstance -ResourceURI winrm/config -ValueSet @{MaxEnvelopeSizekb="1024"}
> ```

### 🧱 Шаг 2: Дополнительные компоненты (в зависимости от конфигурации)
Для определенных сценариев на узлы нужно установить дополнительные роли:

| Компонент | Когда нужен | Как установить (PowerShell) |
| :--- | :--- | :--- |
| **Multipath I/O (MPIO)** | Если используется **общее хранилище** (SAN, iSCSI) для обеспечения отказоустойчивости путей доступа к дискам. | `Install-WindowsFeature Multipath-IO` |
| **Файловые службы (File Services)** | Если планируется поднимать кластерную роль файлового сервера (SMB). | `Install-WindowsFeature FS-FileServer -IncludeManagementTools` |

### 🧪 Шаг 3: Валидация конфигурации
После установки роли на все узлы, **прежде чем создавать кластер**, **обязательно** запустите **мастер валидации**. Это не формальность, а ключевой шаг, который выявит скрытые проблемы с сетью, хранилищем или конфигурацией.

```powershell
# Запуск тестов валидации для двух узлов
Test-Cluster -Node Server1, Server2
```
В отчете валидации обращайте внимание на **ошибки (Failures)** — их нужно исправить. **Предупреждения (Warnings)** стоит изучить, но они могут быть допустимы.

Только после успешного прохождения валидации можно переходить к самому созданию кластера, о чём мы поговорим в следующем разделе.
